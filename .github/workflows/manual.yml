# This is a basic workflow that is manually triggered

name: Auto Dependabot

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "greet"
  autodependabot:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Runs a single command using the runners shell
    - name: Github Action Merge Dependabot
      # You may pin to the exact commit or the version.
      # uses: fastify/github-action-merge-dependabot@79ed7224ebf48f0f9392043db8d37b33696d3332
      uses: fastify/github-action-merge-dependabot@v1.1.1
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        # The merge method you would like to use (squash, merge, rebase)
        merge-method: rebase
