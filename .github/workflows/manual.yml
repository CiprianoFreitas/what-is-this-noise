# This is a basic workflow that is manually triggered

name: Auto Dependabot

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "greet"
  autodependabot:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Runs a single command using the runners shell
    - name: Dependabot Auto Merge
      # You may pin to the exact commit or the version.
      # uses: ahmadnassri/action-dependabot-auto-merge@bacda948a54941e442c216f4c1c84240c8f9c04a
      uses: ahmadnassri/action-dependabot-auto-merge@v2.2.0
      with:
        # The GitHub token used to merge the pull-request
        github-token: ${{ secrets.GITHUB_TOKEN }}
        # The command to pass to Dependabot as a comment

